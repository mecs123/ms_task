server:
  port: ${PORT:8080}

spring:
  application:
    name: task-api-001

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  r2dbc:
    url: jdbc:postgresql://app-task-db.postgres.database.azure.com:5432/postgres?user=postgres&password={Manolo9315308+1}&sslmode=require
    pool:
      initial-size: 1
      max-size: 10
      validation-query: SELECT 1
    username: ${PGUSER:postgres@app-task-db}
    password: ${PGPASSWORD:Manolo9315308+1}
    database: ${PGDATABASE:task}
    host: ${PGHOST:app-task-db.postgres.database.azure.com}
    port: ${PGPORT:5432}

postgres:
  host: ${PGHOST:app-task-db.postgres.database.azure.com}
  port: ${PGPORT:5432}
  database: ${PGDATABASE:task}
  username: ${PGUSER:postgres@app-task-db}
  password: ${PGPASSWORD:Manolo9315308+1}
  schema: ${POSTGRES_SCHEMA:public}

external:
  api:
    base-url: ${EXTERNAL_API_URL:http://localhost:8080}

frontend:
  url: ${FRONT_URL:http://localhost:3000}